<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Careeria</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.13.0/css/all.css">
    <link href="~/Content/Site.css" rel="stylesheet" />
</head>
<body>
  
        @{ 
            if (Session["Sähköposti"] == null)
            {
                Html.RenderPartial("_NavBarLoggedOut");
            }
            else
            {
                Html.RenderPartial("_NavBarLoggedIn");
            }
        }
    @*<section id="mini-banner">
        <div class="container text-center ">
            <div class="row">
               <div class="col-md-12 ">
                    <p class="mini-banner-text"> CAREERIAN KURSSIEN HALLINTA </p>
                </div>
            </div>
        </div>
    </section>*@


    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application - (@TempData["LoggedStatus"]) </p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
